# Absolute minimal set of dependencies - no binary/compiled packages
fastapi>=0.95.0
uvicorn[standard]>=0.21.0
pydantic>=2.4.0
python-dotenv>=1.0.0
requests>=2.28.0
aiohttp>=3.8.0
# Avoid any packages that require compilation
# No SQLAlchemy or database drivers for now
# No complex logging libraries
